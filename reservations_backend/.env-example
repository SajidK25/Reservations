# PostgreSQL connection settings (used by TypeORM or pg)
DB_HOST=postgres         # Hostname of the database server (Docker service name or 'localhost')
DB_PORT=5432             # PostgreSQL default port
DB_NAME=reservations     # Name of the database
DB_USER=postgres         # Database username
DB_PASS=postgres         # Database password

# JWT authentication settings
JWT_SECRET=tajna123      # Secret key used to sign JWT tokens

